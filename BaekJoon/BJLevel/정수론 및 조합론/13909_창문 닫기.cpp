#include <iostream>
#include <cmath>
#include <map>
#include <algorithm>
using namespace std;

void solve(long long int N) {
    long long result = (long long int) sqrt(N);

    cout << result << endl;
}

int main() {

    long long int N;

    cin >> N;

    solve(N);
    return 0;
}